---
name: random_number
description: 生成随机数。当用户需要随机、抽签等时使用。
---

## Parameters

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| max_val | number | 否 | 最大值（默认100） |
| min_val | number | 否 | 最小值（默认0） |
| count | number | 否 | 生成数量（默认1） |
